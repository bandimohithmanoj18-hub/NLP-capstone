# Production backend API URL (optional).
# If left unset or blank, frontend defaults to calling relative '/api/v1'.
# When hosting frontend on Vercel with backend on Render / Railway / Fly.io:
# VITE_API_URL=https://your-backend-service.onrender.com
